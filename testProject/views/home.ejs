<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .home-container {
            max-width: 480px;
            margin: 40px auto;
            background: rgba(255,255,255,0.97);
            border-radius: 12px;
            box-shadow: 0 2px 16px rgba(0,0,0,0.07);
            padding: 32px 24px 24px 24px;
            text-align: center;
        }
        .home-title {
            font-size: 2em;
            color: #2bbecb;
            margin-bottom: 18px;
            font-weight: 700;
        }
        .home-user {
            color: #333;
            font-size: 1.1em;
            margin-bottom: 24px;
        }
        @media (max-width: 600px) {
            .home-container {
                max-width: 98vw;
                padding: 18px 4vw 18px 4vw;
            }
            .home-title {
                font-size: 1.3em;
            }
        }
    </style>
</head>
<body>
    <div class="home-container">
        <div class="home-title">Welcome to the Home Page</div>
        <div style="color:#888;font-size:1em;margin-bottom:18px;">by <b>TRI</b></div>
        <div class="home-user">
            Hello, <span><%= user ? user.firstName : 'User' %></span>!<br>
            You have successfully logged in.
        </div>
        <a href="/logout" class="submit-btn" style="background:#ff6b6b;">Logout</a>
    </div>
</body>
</html>